Control your devices using Join.

Uses the API provided by joaoapps to, amongst other things, send notifications, toggle 'do not disturb' and trigger Tasker or EventGhost tasks.

You can install the Join app on various device types but not all actions work on all types.

Go into Homey settings, and under Join follow the instructions to set your Join API token.